Title and pitch one sentence stating what it is.
Purpose the problem it solves and the approach (the three agents and their roles).
Quick start install and run commands, plus exit code meanings.
Example output  a real sample run.
Architecture  a diagram and a table of agents, their inputs, decisions, and tools.
Configuration tunable logic such as the risk formula and weights.
Safety  guardrails on automatic actions: dry-run enforcement, blocked tools, no auto-fixing failing tests.
Optional features anything not required for core use, like the LLM backend.
Extending it  short code snippets for adding a tool or agent.
Project layout a file tree with one-line descriptions.
Development  commands to install, test, and lint.
Known limits  platform support, untested paths, network dependencies.
License — missing from the current README; add a line pointing to the license file.
